#include <stdio.h>
#include <stdlib.h>
#include <conio.h>

int main()
{
    /*int x,y;
    printf("Enter the first number: ");
    scanf("%d",&x);
    printf("Enter the second number: ");
    scanf("%d",&y);
    printf("the sum is: %d",x+y);*/

    char ch;
    int x;
    printf("enter a character:");
    x=getch(); //ch=getche();ch=getchar();
    printf("%d",x);
    return 0;
}

